# ===== 必装 =====
playwright>=1.45          # 驱动浏览器登录 WebVPN、点菜单、读 iframe
beautifulsoup4>=4.12      # 解析「最近访问记录」表格
lxml>=5.0                 # bs4 的高速解析器（没有也能跑，但慢且更容易解析歪）
requests>=2.31            # IP 查询 + 各种推送通道
PyYAML>=6.0               # 读 config.yaml

# ===== 强烈建议 =====
# 验证码自动识别。不装的话，每次会话过期都要人工输一次验证码，就没法真正无人值守了。
# 注意：1.6.0 是坏的（import 就报 NameError），必须 >=1.6.1。
# 体积较大（连同 onnxruntime/opencv 约 400MB），介意的话可以不装，改用人工输入。
ddddocr>=1.6.1,!=1.6.0

# ===== 可选 =====
# 离线 IP 库。装了之后国内 IP 能精确到「区/县」，而且零延迟、不耗 API 配额。
# 两个库配合最好：GeoCN 管大陆（到区县），ip2region 管全球（到市）。
# 库装完还要单独下数据文件，见 README「IP 定位精度」一节。
maxminddb>=2.6          # 读 GeoCN.mmdb
py-ip2region>=3.0.4     # 读 ip2region.xdb（官方绑定；注意不是 XdbSearchIP）
