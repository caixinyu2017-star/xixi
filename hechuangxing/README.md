# 禾创星 · 嘉兴本地化创业智能体

一款面向嘉兴本地大学生与小微初创企业的创业智能体：上传创业计划书或直接说思路，它先判断这个方案在嘉兴能不能吃到政策，够不上就直接说够不上，然后给出把方案改造成能享受政策的版本，并一键导出两份排版合规的 Word。

文化底色是嘉兴南湖的红船精神与海盐步鑫生的改革创业精神。

---

## 一、三分钟跑起来

### Windows

双击 `启动.bat`，等它自己装完依赖，浏览器会自动打开 <http://127.0.0.1:8848>。

### macOS / Linux

```bash
cd hechuangxing
./启动.sh
```

然后浏览器打开 <http://127.0.0.1:8848>。

### 手动启动（任何系统）

```bash
python3 -m venv .venv
./.venv/bin/pip install -r requirements.txt
./.venv/bin/python server.py
```

> 需要 Python 3.10 以上。首次安装依赖大约 1—2 分钟。

---

## 二、要不要配 API Key

两种模式都能完整演示，区别只在回答是现场生成还是内置脚本。

| 模式 | 怎么进入 | 表现 |
| --- | --- | --- |
| 离线演示模式 | 不配 Key，或在 `.env` 里设 `HCX_DEMO=1` | 用内置的嘉兴案例脚本流式作答，两份 Word 用内置内容生成。断网也能跑，录屏零风险 |
| 实时模式 | 在 `.env` 里填 `ANTHROPIC_API_KEY` | 接 Claude 实时生成，回答与 Word 内容都按你上传的真实计划书现写 |

配置方法：把 `.env.example` 复制成 `.env`，填上这一行

```
ANTHROPIC_API_KEY=sk-ant-你的key
```

如果用的是中转服务，再加一行 `ANTHROPIC_BASE_URL=你的地址`。

**录屏建议**：先用离线演示模式走一遍流程确认画面，正式录制时再切实时模式；万一现场网络出问题，把 `HCX_DEMO=1` 打开即可继续。

---

## 三、界面怎么用

- **中间**：聊天区。左下角回形针按钮上传创业计划书，也支持直接把文件拖进窗口、或者截图后 Ctrl+V 粘贴。支持 `.docx`、`.pdf`、`.txt`、`.md` 和图片。
- **左栏**：全链条八个环节，点任意一个直接发出对应的问题；下面是当前加载的专属语料库清单。
- **右栏**：
  - **可下载交付物**：两个按钮分别生成《创业计划改进方案》和《全链条创业落地方案》Word，生成后在下方出现下载链接。
  - **政策雷达**：根据你说的内容实时命中政策，显示金额、层级与主管部门。
  - **示例文件**：三份预生成的 Word 样张，可直接下载查看排版效果。

**演示动线建议**：上传 `samples/测试用-网媒投稿发表公司创业计划书.docx` → 发一句「帮我看看这个方案能不能拿到嘉兴的政策，该怎么改」→ 等回答出完 → 点右侧两个按钮生成 Word → 打开 Word 展示排版。

---

## 四、Word 排版规范

两份输出文档与测试用计划书都按下列规范渲染，已通过逐段自动核验：

| 层级 | 中文字体 | 西文字体 | 字号 | 对齐 | 首行缩进 |
| --- | --- | --- | --- | --- | --- |
| 一级标题 | 黑体 | Times New Roman | 小三（15pt） | 居中 | 无 |
| 二级标题 | 黑体 | Times New Roman | 四号（14pt） | 左对齐 | 2 字符 |
| 三级标题 | 宋体加粗 | Times New Roman | 小四（12pt） | 左对齐 | 2 字符 |
| 正文 | 宋体 | Times New Roman | 小四（12pt） | 两端对齐 | 2 字符 |
| 表格 | 宋体 | Times New Roman | 五号（10.5pt） | 居中 | 无 |

正文与标题一律无段前段后间距；表格段前段后 0 磅、单倍行距、整表居中、表头跨页重复；首行缩进用 Word 的字符单位（`firstLineChars=200`），换字号不会走样；引号等标点跟随中文字体。

正文行距默认 1.5 倍（规范只对表格明确要求单倍行距）。要改成单倍，编辑 `hcx/docx_style.py` 里的一行：

```python
BODY_LINE_SPACING = 1.5   # 改成 1.0 即为单倍行距
```

自检命令：

```bash
./.venv/bin/python scripts/check_format.py
```

它会逐段核对字体、字号、对齐、缩进、段前段后与表格行距，输出问题清单。

---

## 五、目录结构

```
hechuangxing/
├── 启动.sh / 启动.bat        一键启动
├── server.py                后端服务（FastAPI）
├── requirements.txt
├── .env.example             配置模板
├── corpus/                  专属语料库
│   ├── 00_文化底色_红船精神与步鑫生.md
│   ├── 01_创业启蒙与主体选择.md
│   ├── 02_商业模式打磨.md
│   ├── 03_工商注册全流程.md
│   ├── 04_开户行比选.md
│   ├── 05_税务与社保.md
│   ├── 06_政策匹配库.md
│   ├── 07_融资对接.md
│   ├── 08_企业成长复盘.md
│   ├── 09_嘉兴产业地图与选址.md
│   ├── 10_行业资质与风险预警.md
│   ├── 11_服务话术与输出规范.md
│   └── policies.json        26 条结构化政策（政策雷达用）
├── hcx/
│   ├── config.py            配置与 .env 读取
│   ├── corpus.py            语料检索 + 政策匹配打分
│   ├── prompts.py           禾创星角色设定与文档生成提示词
│   ├── llm.py               Claude 接入（流式对话 + 结构化文档）
│   ├── extract.py           附件解析 docx / pdf / txt / 图片
│   ├── docx_style.py        Word 排版引擎（本项目的核心）
│   ├── demo.py              离线演示模式
│   ├── content_bp.py        测试用创业计划书内容
│   ├── content_improve.py   改进方案内容
│   └── content_landing.py   落地方案内容
├── web/                     前端（原生 HTML/CSS/JS，无需构建）
├── samples/                 三份预生成 Word 样张
├── scripts/
│   ├── generate_samples.py  重新生成样张
│   └── check_format.py      排版合规自检
├── uploads/                 上传的附件（本地，不入库）
└── outputs/                 生成的 Word（本地，不入库）
```

---

## 六、禾创星和通用大模型的区别

通用大模型回答创业问题时给的是放之四海皆准的建议。禾创星强制走一套本地化的判断流程：

1. **政策适配性四问**：有没有自主知识产权；收入里有没有软件或技术成分；行业代码归到哪一类；能不能挂上嘉兴主抓的产业方向。四问过一遍，能拿什么、拿不到什么，一清二楚。
2. **敢下否定结论**：够不上就说够不上，缺哪个条件、怎么补，写明白。
3. **给数字不给形容词**：写清具体金额、比例、期限、主管部门与文件出处。
4. **给动作不给建议**：写清下周几、带什么材料、去哪个部门办。
5. **风险预警前置**：例如网媒投稿类业务，会先告诉你互联网新闻信息服务许可证普通商业公司拿不到，哪些活不能接。

---

## 七、语料库来源

语料库内容整理自国家、浙江省与嘉兴市公开政策文件与官方报道，主要包括：

- 嘉兴市进一步做好新时代就业创业工作若干政策措施
- 嘉兴市推动经济高质量发展若干政策（2025 年版）（嘉政发〔2025〕6 号）
- 嘉兴市推动人工智能高质量发展实施方案、嘉兴市推进人工智能加行动计划（2025—2027 年）
- 浙江省关于支持人工智能创新发展的若干措施
- 浙江省科技型中小企业认定管理相关通知
- 高新技术企业认定管理办法及国家重点支持的高新技术领域
- 财政部与税务总局关于小微企业和个体工商户税费优惠的公告
- 嘉兴市本级大学生 550 引才实施意见操作细则
- 嘉兴科创金融改革试验区相关举措与公开报道
- 互联网新闻信息服务、网络文化经营、增值电信业务等资质管理规定

**重要提醒**：政策会调整，补贴多为限时受理。语料库中的金额、条件与期限以公开文件表述为准，正式申报前务必以属地主管部门最新公告为准，或直接致电对应部门核实。禾创星在回答中也会同步提示这一点。

---

## 八、常见问题

**上传 .doc 提示读不了？**
Word 旧版格式无法直接解析，在 Word 里另存为 .docx 再传，或者把正文粘贴到对话框。

**PDF 传上去是空的？**
扫描件没有文字层，提取不到内容。上传 Word 版本或粘贴正文。

**端口被占用？**
在 `.env` 里改 `HCX_PORT=8849`（或其他端口），重新启动。

**生成 Word 时报错？**
实时模式下多为 API Key 或网络问题，界面会把错误原文显示出来。把 `HCX_DEMO=1` 打开可立即切回离线演示模式继续演示。

**想换模型？**
改 `.env` 里的 `HCX_MODEL`。
