# 仓库约定

## 中文论文／专著／课题申请书

**凡在本仓库撰写或修改中文论文、专著章节、课题申请书，一律先读 `STYLE.md` 并严格执行。**
该文件是长期生效的成稿规格，涵盖字体字号、公式（Word 原生公式对象、编号右顶格）、
图表体例（图题在下、表题在上、三线表）、参考文献（GB/T 7714 顺序编码制、起止页码齐全、
多数为 2022 年以后、必须真实且经联网核验）以及语言与标点要求。

不要每次重新询问这些格式约定，也不要在未读 `STYLE.md` 的情况下直接产出稿件。

## 成稿流水线

各论文项目位于 `work/<项目名>/`，统一采用以下流水线：

```
src/*_sim.py      蒙特卡洛仿真数据生成与全部计量估计，结果写入 results/*.json
src/make_figs*.py 插图绘制（数据一律取自 results/）
src/build*.py     引文占位符 [@key] → 顺序编码，并生成参考文献表
pandoc            markdown → docx（LaTeX 数学转为 OMML 原生公式对象）
src/finalize.py   docx 的期刊化排版后处理，按 STYLE.md 执行
src/validate.py   正文中每一个数字与 results/*.json 的逐一比对
```

硬性要求：

- **论文中的每一个数字都必须由脚本实际估计得到**，不得人工编写；定稿前必须跑
  `src/validate.py`，做到"无孤儿数字"。
- 计量估计器尽量按原始文献自行实现，并在脚本头部注释中列明所依据的文献。
- 参考文献的联网核验记录写入各项目的 `refs_verification.md`；核验未通过的一律不采用。

## 交付物

各项目的 `out/paper*.docx` 为定稿交付文件，由上述流水线生成，不要手工编辑；
需要修改时改 `src/*.md` 或 `src/finalize.py` 后重新构建。
