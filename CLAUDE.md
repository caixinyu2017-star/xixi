# 本仓库：中文经济学专著书稿工程

四部书稿各自独立成目录，共用同一条流水线：
`content_*.py`（块字典）→ `docxbuilder.py`（python-docx ＋ pandoc LaTeX→OMML）→ `assemble.py`；
`audit.py` 做结构审计，`data_gen.py` 产出可复现的量化结果。

| 目录 | 书名 |
|---|---|
| `book/` | 全国统一大市场建设与要素配置效率 |
| `green-standards-book/` | 双碳目标下中国绿色低碳标准体系研究 |
| `ai-talent-book/` | 面向人工智能开发应用领域需求的青年科技人才产教融合培育机制研究 |
| `token-econ-book/` | 标准词元与智能经济——Token 的价值计量、定价机制与配置效率研究 |

每本书的 `AGENTSPEC.md` 是撰写与排版的硬性规范，动手前必读；
`DESIGN.md` 是该书的骨架与选题论证。

## 交付前必须逐条核对的体例（用户已两次点名，再犯即返工）

1. **字体**：正文中文宋体、西文 Times New Roman；标题中文黑体、西文仍为 Times New Roman。
   `w:rFonts` 必须带 `w:hint="eastAsia"`，否则引号“”‘’、破折号——、省略号……
   会被 Word 判给西文字体，排出半角直引号。
2. **表格**：单元格段前 0 磅、段后 0 磅、单倍行距、水平与垂直居中、首行缩进 0、
   关闭 snapToGrid。表内文字要短，避免居中折行参差。
3. **引号**：全书禁用直角引号「」『』，一律中文引号 “ ”／‘ ’，外双内单。
   **宁缺勿滥**：只用于直接引语、政策专有提法与口号、反语与非字面义、自造概念首次出现。
   核心术语与普通名词的强调不加引号。标准名／榜单名／文件名用《》。
   判据：删掉引号后含义不变且不会误读，就删。
4. **术语**：同一概念全书只用一个通行词形，经济学术语尤须核准，不得同义换用求文采。
   首次出现给中文全称＋（英文全称, 缩写），其后只用简称。
5. **图内字号**：按 13.5—15 cm 版心印刷，图内最小字符高度不得低于图高的 1/28
   （折合 ≥7.5 pt），框内留白收紧，严禁「大框小字」。
   机制／框架概念图（D 型）用 `gpt-image-2` 技能生成；带真实数据的统计图（A 型）
   用 matplotlib 并绑定 results.json。注意：印刷字号＝源字号 × 版心宽 ÷ 画布宽，
   matplotlib 画布开得越宽，印出来的字越小。
6. **图表注**：按学术惯例宁缺勿滥。只承载正文无法承载的元信息
   （数据来源、统计口径、显著性、图内简称对应、符号约定、复现参数）；
   正文已交代的内容不设注；作者自绘的纯概念框架图不补「资料来源：作者绘制」。
7. **排版铁律**：先文后图表；图名在下、表名在上；图表按出现顺序编号；
   公式为 OMML 原生对象、居中、编号 (章-序) 右对齐；参考文献 GB/T 7714，
   正文引注 （作者，年份），合引年份升序。

## 事实纪律

- 数字一律来自 `data/facts.md`（核实过的公开事实）或 `results.json`（代码算出的结果），
  不得手写编造；模型档位用抽象类别名，不挂靠具体商业产品。
- 用户提供的素材若与公开信息冲突，先核实、再更正，并把更正留档在该书 `DESIGN.md` 里。

## Git

只在指定的功能分支上开发与推送，不要推到其它分支。
