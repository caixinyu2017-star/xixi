# 项目约定

## 学术图表规范（所有对话必须遵守）

以下规则来自作者确认的图表修订要求（范例：国自然申请书终稿、《管理世界》系统动力学论文与案例研究论文），适用于本仓库所有学术文稿的图表制作与修改。

### 图片生成方式

- 一律使用 image2 模型（`gpt-image-2`）生成，API 为 `https://chedankj.com/v1/images/generations`，
  密钥在环境变量 `CHEDANKJ_API_KEY`（必须用 curl 调用，urllib/requests 会被 Cloudflare 拦截，报 403 error 1010）。
- 现成脚本在 `双碳标准体系专著-图表修订/scripts/`：figspec.py（逐图版式规范）→ run_gen.py（并行生成+重试）
  → autocrop.py（裁除多余留白）→ replace_imgs.py（替换进 docx 并按宽高比更新版式尺寸）→ postprocess.py（图注+三线表）。
- 生成后必须逐字校对图内文字、逐条核对连线与极性，不合格重新生成。
  已知易错字：“阈”会被写成“阀”（可改用等价表述规避）、“话语权”曾被写成“话证权”（可用 AR PL UMing 字体局部重绘修补）。
- 所有图片按内容边界裁除多余上下留白后，以原生成像素高清插入，不得缩放降质。

### 图型风格

- **机制图/路线图/路径图/框架图**：黑白学术风——直角矩形框（不用圆角）、虚线框分组、横平竖直的直线或直角折线箭头；
  严禁曲线、严禁图标、严禁彩色与阴影；元素不得遮挡重叠；简洁明了、点明逻辑关系即可。
- **系统动力学结果**（因果回路图、存量-流量图）：按 Vensim 导出图形式呈现——变量为无框文字、蓝色因果箭头带＋/－极性、
  R/B 回路标记；存量-流量图用矩形存量、蝶形阀流量、云朵源汇、蓝色信息链、红色虚线更新反馈。
- **时间轴/历程图**：中部浅灰阶段带+右端箭头，事件框上下穿插、竖直箭头指向阶段带，年份紧贴落点，阶段间贯穿虚线分隔。
- **层级结构**：金字塔形（如四层级标准结构）；AHP 结果按“目标层—准则层—方案层”完全连接层次图的学术规范画法。
- **数据图**：可用彩色；同族图必须统一色系（本书基准配色：深蓝 #2E608C + 橄榄绿 #5B8F4D）；
  坐标轴、锚点数值、柱高与标注值必须与正文完全一致，逐一人工核对。

### 图注与表格

- 图内一律不得出现“注：”字样；注文放在图标题的下一行，格式与表注一致：宋体 9 磅（小五）、无缩进、单倍行距。
- 全文表格一律三线表：顶线与底线粗（1.5磅）、栏目线细（0.75磅），无竖线、无内横线、无底纹；
  表内文字统一五号（10.5磅）、单倍行距。

### 文档处理

- 修改 docx 只替换图片与版式，正文文字不动；改后用 LibreOffice 渲染 PDF 逐页目检改动处
  （环境若报 “source file could not be loaded”，需先 `apt-get install libreoffice-writer libreoffice-draw poppler-utils`）。
- 成果放在 `双碳标准体系专著-图表修订/`（修订稿 docx、figures/ 按图号命名的 PNG、scripts/、README.md）。
