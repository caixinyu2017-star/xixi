# 中文论文／专著／课题申请书的统一排版与著录规范

本文件是长期生效的成稿规格。**所有中文论文、专著章节与课题申请书一律按此执行**，
无需每次重新约定。实现见各项目 `src/finalize.py`（对 pandoc 生成的 docx 作后处理）。

## 一、字体与字号

| 层级 | 字体 | 字号 | 字重 |
|:---|:---|:---|:---|
| 论文题名 | 黑体 | 小二（18 pt） | 加粗 |
| 一级标题（章） | 黑体 | 小三（15 pt） | 加粗 |
| 二级标题（节） | 黑体 | 四号（14 pt） | 不加粗 |
| 三级及以下标题（条） | 黑体 | 小四（12 pt） | 不加粗 |
| 正文 | 宋体 | 小四（12 pt） | —— |
| 图名、表名 | 黑体 | 五号（10.5 pt） | —— |
| 表格内容 | 宋体 | 五号（10.5 pt） | —— |
| 表注、图注 | 宋体 | 小五（9 pt） | —— |
| 参考文献条目 | 宋体 | 五号（10.5 pt） | —— |

- 西文与数字一律 Times New Roman。
- **正文段前、段后间距一律为 0**；行距 1.5 倍；首行缩进 2 字符。
- pandoc 默认模板会给 `Body Text` 加 9 磅段前距，必须在样式层与段落层双重清除。

## 二、公式

- 一律使用 **Word 自带公式编辑器的原生对象（OMML）**，不得用图片或文本框拼装。
  技术路径：markdown 中写 LaTeX 数学，由 pandoc 转为 OMML。
- 公式**居中**排版。
- 公式**编号右顶格**（右对齐于版心右边界），用圆括号括起，形如 `(2-1)`，
  即"章序号－该章内公式序号"。
- 实现：公式段设两个制表位——版心中点处的居中制表位、版心右边界处的右对齐制表位；
  段落结构为 `TAB + 公式 + TAB + 编号`，段落左对齐、无首行缩进。
- markdown 源码写法：公式块 `$$...$$` 之后紧跟单独一段 `(2-1)`，
  由 `finalize.py` 合并为一段并加制表位。
- 多字母变量名用 `\operatorname{}` 包裹，避免 OMML 把它拆成逐字母斜体。

## 三、图与表

- **图名置于图的下方**，**表名置于表的上方**，均为五号黑体、居中。
- 表格一律为**三线表**：顶线与底线 1.5 磅、栏目线 0.75 磅，
  无竖线、无其余横线。表格满栏宽、行不跨页断开、首行跨页时重复。
- 列宽按内容计算：先保证各列中最长的不可断串（如 `−0.0173***`、`（−3.7924）`、`W×aging`）
  不被拆行，余量再按显示宽度需求比例分配，并对单列封顶以免首列独占版面。
- 表注置于表下方，以"注："起首。
- **图例不得遮挡数据**：置于数据稀疏区或图外；点、线、误差棒等图元的含义须在图注中写明。

## 四、参考文献

采用国家标准《文后参考文献著录规则》（GB/T 7714）的**顺序编码制**：按正文引用先后连续编码，
正文中以上标 `[1]`、`[2]` 标注。

著录格式（**起止页码必须齐全**）：

- 专著：`[序号] 作者. 书名[M]. 出版地: 出版者, 出版年: 起止页码.`
- 期刊：`[序号] 作者. 文章名[J]. 期刊名, 出版年, 卷(期): 起止页码.`
- 学位论文：`[序号] 作者. 题名[D]. 保存地: 保存者, 年份: 起止页码.`
- 三位以上作者列前三位后加"等"（英文用 `et al.`）。

硬性要求：

1. **文献必须真实存在**，且每一条都经过联网核验；核验痕迹记入 `refs_verification.md`。
2. **正文中出现的每一处引用，必须能在参考文献表中找到**；反之文献表不得有正文未引用的条目。
3. **2022 年及以后发表的文献应占多数**（不低于 60%）。
   口径：分母为**主题文献**（与研究对象直接相关的实证与理论文献）。
   **方法文献**——即估计量、检验统计量、指数构造方法的原始出处（如 Hansen 1999
   的门槛模型、Callaway & Sant'Anna 2021 的双重稳健估计量、LeSage & Pace 2009 的
   效应分解、Chung 等 1997 的方向性距离函数）——不计入分母：这类文献的年份由方法
   本身的提出时间决定，不可替换，也不应以补充无关近作的方式凑足比例。
   凡引用方法文献，必须在正文中说明所采用的具体设定，不得只作装饰性引用。
4. 页码、卷期、年份三项中任何一项无法由两个及以上独立来源相互印证的，一律不予采用；
   核验未通过的条目须连同未通过的原因一并记入 `refs_verification.md`，
   并在正文中改引可核验的替代文献。

## 五、语言

- 全文中文；**所有标点符号使用中文全角**（公式编号的圆括号按上文示例用半角）。
- 使用规范的中文学术用语与学术分析逻辑；避免口语化表述与翻译腔。
- 论证需完整：提出假设→给出理论机制→说明识别策略→报告检验→解释结果边界。
